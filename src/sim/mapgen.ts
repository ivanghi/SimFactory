import { WorldState } from './world';

export function generateMap(width: number, height: number): WorldState {
  // Implementation placeholder - will be filled in later
  return {
    buildings: [],
    resources: {},
    power: {
      produced: 0,
      consumed: 0
    }
  };
}