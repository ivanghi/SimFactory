import { WorldState } from './world';

export function processOfflineTime(state: WorldState, offlineTimeMs: number): WorldState {
  // Implementation placeholder - will be filled in later
  return state;
}